spring:
  jpa:
    hibernate:
      ddlAuto: update

  datasource:
    url: jdbc:mysql://localhost:3306/{Database}
    username: {name}
    password: {password}
    driverClassName: com.mysql.cj.jdbc.Driver